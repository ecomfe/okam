/**
 * @file Swan filter
 * @author xxx
 */

// require other filter is not allowed
// using module.exports = {} is not allowed
// export string const is not allowed, function is the only type to support
export default {
    toUpperCase(str) {
        return str.toUpperCase();
    }
}


