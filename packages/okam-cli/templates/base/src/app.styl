/**
 * @file The app entry style
 * @author ${author|raw}
 */

<% var: size = ${px2rpx} ? 150 : 50 %>
<% if: ${style} === 'stylus' %>

.page-img
    margin: 0 auto
    display: block
    width: ${size}px
    height: ${size}px
.page-footer
    position: absolute
    bottom: 80px
    width: 100%
    text-align: center
<% else %>

.page-img {
    margin: 0 auto;
    display: block;
    width: ${size}px;
    height: ${size}px;
}
.page-footer {
    position: absolute;
    bottom: 80px;
    width: 100%;
    text-align: center;
}
<% /if %>
